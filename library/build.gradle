apply plugin: 'com.android.library'

android {
    compileSdkVersion rootProject.ext.compileSdkVersionValue as Integer
    buildToolsVersion rootProject.ext.buildToolsVersionValue as String

    defaultConfig {
        minSdkVersion 14 as Integer
        targetSdkVersion rootProject.ext.compileSdkVersionValue as Integer
        versionCode 1 as Integer
        versionName "1.0.0" as String
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
}

dependencies {
    compile 'com.android.support:recyclerview-v7:22.0.0'
}

apply from: 'gradle-maven-push.gradle'

afterEvaluate {
    androidJavadocs.classpath += project.android.libraryVariants.toList().first().javaCompile.classpath
}
